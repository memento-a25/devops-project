- name: Deploy Application
  hosts: all
  become: yes
  vars:
    project_dir: "/home/github-runner/devop-blog-project"

  tasks:

    - name: Stop and remove old containers
      command: docker compose -f {{ project_dir }}/docker-compose.yml down
      args:
        chdir: "{{ project_dir }}"
      ignore_errors: yes
  
    - name: Pull latest images
      command: docker compose -f {{ project_dir }}/docker-compose.yml pull
      args:
        chdir: "{{ project_dir }}"
        
    - name: Ensure containers are running
      command: docker compose -f {{ project_dir }}/docker-compose.yml up -d --build
      args:
        chdir: "{{ project_dir }}"
